OnAuthEvent(event: AuthChangeEvent): boolean {
+  return event !== "TOKEN_REFRESHED";
+}
+
+/**
+ * Whether an AppState transition should trigger a buyer eligibility
+ * refresh. Only the transition INTO "active" (foreground) matters --
+ * "background"/"inactive" transitions have nothing to refresh (the app
+ * isn't visible), and refreshing on every state value AppState reports
+ * (which includes transitional states beyond just active/background on
+ * some platforms) would be wasteful. This is what catches a buyer whose
+ * company was frozen, or who was de-approved, while the app was
+ * backgrounded or the device was locked -- see BuyerSessionContext.tsx for
+ * why onAuthStateChange alone can't catch that case.
+ */
+export function shouldRefreshOnAppState(nextState: AppStateStatus): boolean {
+  return nextState === "active";
+}
diff --git a/src/lib/checkout-submit-error.test.ts b/src/lib/checkout-submit-error.test.ts
new file mode 100644
index 0000000..f74857f
--- /dev/null
+++ b/src/lib/checkout-submit-error.test.ts
@@ -0,0 +1,32 @@
+import { describe, it } from "node:test";
+import assert from "node:assert/strict";
+import { classifyCheckoutSubmitError } from "./checkout-submit-error";
+import type { ParsedRpcError } from "./rpc-errors";
+
+describe("classifyCheckoutSubmitError", () => {
+  it("DRAFT_NOT_FOUND: shows the concurrent-promotion message and sets concurrentPromotion true", () => {
+    const parsed: ParsedRpcError = { code: "DRAFT_NOT_FOUND", message: "No active order draft was found. Add items from the catalogue." };
+    const result = classifyCheckoutSubmitError(parsed);
+    assert.equal(result.concurrentPromotion, true);
+    assert.match(result.message, /may have just been submitted/i);
+    assert.match(result.message, /Check Orders/i);
+    // Must NOT surface the RPC's own generic message here -- it would
+    // wrongly imply the cart was always empty.
+    assert.doesNotMatch(result.message, /Add items from the catalogue/);
+  });
+
+  it("unrelated errors are never mislabeled as concurrent promotion -- regression for classifying " +
+    "by structured code, not string-matching on message text", () => {
+    const unrelatedCodes: ParsedRpcError[] = [
+      { code: "DRAFT_NOT_READY", message: "Your cart is not ready for checkout. Review quantity rules below." },
+      { code: "PRODUCT_UNAVAILABLE", message: "This product is not available for your account right now." },
+      { code: "AUTH_REQUIRED", message: "Please log in again." },
+      { code: "UNKNOWN", message: "Something went wrong. Please try again." },
+    ];
+    for (const parsed of unrelatedCodes) {
+      const result = classifyCheckoutSubmitError(parsed);
+      assert.equal(result.concurrentPromotion, false, `"${parsed.code}" must not be classified as concurrent promotion`);
+      assert.equal(result.message, parsed.message, `"${parsed.code}" must pass through its own message unchanged`);
+    }
+  });
+});
diff --git a/src/lib/checkout-submit-error.ts b/src/lib/checkout-submit-error.ts
new file mode 100644
index 0000000..fa9abbd
--- /dev/null
+++ b/src/lib/checkout-submit-error.ts
@@ -0,0 +1,34 @@
+import type { ParsedRpcError } from "@/lib/rpc-errors";
+
+export interface CheckoutSubmitErrorPresentation {
+  message: string;
+  concurrentPromotion: boolean;
+}
+
+const CONCURRENT_PROMOTION_MESSAGE =
+  "This cart may have just been submitted -- by you on another device, or a teammate on your account. Check Orders for the new Sales Order before adding these items again.";
+
+/**
+ * Classifies a checkout-submit failure for display. Uses the structured
+ * error code from parseRpcError, not string-matching on the message text
+ * (which would be fragile and could misclassify an unrelated failure that
+ * happens to mention "draft" in its message).
+ *
+ * DRAFT_NOT_FOUND from submit_customer_order_v1 is ambiguous by itself --
+ * it fires both for a genuinely empty cart AND for a shared company draft
+ * that a concurrent submission (another device, another team member, or a
+ * retried request on this same device) already promoted to an order
+ * moments earlier. Both are represented as the SAME code because that's
+ * what the RPC returns; the surrounding call context (this is the
+ * checkout-SUBMIT action, not add-to-cart) is what licenses treating it as
+ * the concurrent-promotion case here specifically -- callers elsewhere in
+ * the app that hit DRAFT_NOT_FOUND from a different action (e.g. add-to-
+ * cart, where the draft genuinely doesn't exist yet) should NOT use this
+ * classifier; they should show the RPC's own default message instead.
+ */
+export function classifyCheckoutSubmitError(parsed: ParsedRpcError): CheckoutSubmitErrorPresentation {
+  if (parsed.code === "DRAFT_NOT_FOUND") {
+    return { message: CONCURRENT_PROMOTION_MESSAGE, concurrentPromotion: true };
+  }
+  return { message: parsed.message, concurrentPromotion: false };
+}
diff --git a/src/screens/CheckoutScreen.tsx b/src/screens/CheckoutScreen.tsx
index 2f09e55..bde38e8 100644
--- a/src/screens/CheckoutScreen.tsx
+++ b/src/screens/CheckoutScreen.tsx
@@ -14,6 +14,7 @@ import { validateDraftLinesAgainstPrices } from "@/lib/buyer-commercial-validati
 import { clearCheckoutIdempotencyKey, resolveCheckoutIdempotencyKey } from "@/lib/checkout-idempotency";
 import { formatAdvanceDisplay, isCheckoutSubmitEnabled, type AdvanceLoadState } from "@/lib/checkout-submit-guards";
 import { parseRpcError } from "@/lib/rpc-errors";
+import { classifyCheckoutSubmitError } from "@/lib/checkout-submit-error";
 import type { BuyerProductPrice, CustomerOrderDraft } from "@/types/database.types";
 import { colors, spacing, typography } from "@/theme";
 
@@ -138,27 +139,9 @@ export function CheckoutScreen({ navigation }: Props) {
         },
       });
     } catch (e) {
-      const parsed = parseRpcError(e);
-      if (parsed.code === "DRAFT_NOT_FOUND") {
-        // Distinguish "someone already submitted this shared cart" (this
-        // company's active draft was legitimately promoted to an order by a
-        // concurrent submission -- another team member, or this same device
-        // on a retry after a dropped response) from a genuinely empty cart.
-        // Both surface as DRAFT_NOT_FOUND from submit_customer_order_v1, but
-        // the generic "No active order draft was found. Add items from the
-        // catalogue." message is misleading here: it implies the cart was
-        // always empty, when in fact an order likely WAS just created. There
-        // is no reliable way to look up which order resulted (the other
-        // submitter used a different idempotency key), so the honest,
-        // actionable recovery is to point the buyer at Orders rather than
-        // imply they need to re-add everything.
-        setDraftAlreadySubmittedElsewhere(true);
-        setError(
-          "This cart may have just been submitted -- by you on another device, or a teammate on your account. Check Orders for the new Sales Order before adding these items again."
-        );
-      } else {
-        setError(parsed.message);
-      }
+      const presentation = classifyCheckoutSubmitError(parseRpcError(e));
+      setDraftAlreadySubmittedElsewhere(presentation.concurrentPromotion);
+      setError(presentation.message);
     } finally {
       submitInFlightRef.current = false;
       setSubmitting(false);
-- 
2.43.0


From 7e6c0b4080a6aa43ece750c4c0ab8730dba8f45c Mon Sep 17 00:00:00 2001
From: AUTH-01 Native Convergence <auth01-agent@local>
Date: Thu, 17 Sep 2026 21:13:50 +0000
Subject: [PATCH 10/11] fix(support): pass idempotency key to
 submit_customer_support_ticket_v1

Companion native-app change for Core commit de640c6 (branch
core-support-ticket-idempotency), which added a REQUIRED
p_idempotency_key parameter to submit_customer_support_ticket_v1 and
dropped the old unprotected 5-argument signature entirely. Without this
change the Buyer app's ticket submission would simply fail against the
updated Core function once that migration ships -- this is not optional
polish, it's required for the fix to work at all.

- Add support-ticket-idempotency.ts: getSupportTicketIdempotencyKey() /
  clearSupportTicketIdempotencyKey(), mirroring
  general-query-idempotency.ts's proven pattern exactly (persist a key
  across a lost/timed-out response so a retry reuses it instead of
  generating a new one; rotate only after Core acknowledges).
- Update SubmitSupportTicketInput (idempotencyKey field) and add
  SubmitSupportTicketResult (ticket_id, is_duplicate_submission) in
  database.types.ts, matching the new RPC's TABLE return shape (was a
  bare uuid).
- customerGateway.ts submitTicket now passes p_idempotency_key and
  returns the structured result instead of a raw string.
- SupportScreen.tsx's submitOrderTicket: fetches the key before
  submitting, clears it after Core acknowledges, and shows 'This support
  request was already received. We have not created a duplicate.' when
  is_duplicate_submission is true -- exactly the same pattern already used
  for general queries in this same screen.

Add support-ticket-idempotency.test.ts (2 assertions), mirroring
general-query-idempotency.test.ts.

Verified: npm run quality passes end to end -- 172/172 tests (2 new),
typecheck clean, lint clean, boundary/rc-scan/rc-navigation/cert:bindings/
census:p106/expo-config all pass.

No production mutation of any kind.
---
 package.json                               |  2 +-
 src/lib/support-ticket-idempotency.test.ts | 29 +++++++++++++++
 src/lib/support-ticket-idempotency.ts      | 43 ++++++++++++++++++++++
 src/screens/SupportScreen.tsx              | 12 +++++-
 src/services/customerGateway.ts            | 10 ++++-
 src/types/database.types.ts                |  9 ++++-
 6 files changed, 100 insertions(+), 5 deletions(-)
 create mode 100644 src/lib/support-ticket-idempotency.test.ts
 create mode 100644 src/lib/support-ticket-idempotency.ts

diff --git a/package.json b/package.json
index 18218a5..7f0be47 100644
--- a/package.json
+++ b/package.json
@@ -10,7 +10,7 @@
     "web": "expo start --web",
     "typecheck": "tsc --noEmit",
     "lint": "eslint . --max-warnings 0",
-    "test": "node --import tsx --test src/lib/checkout-idempotency.test.ts src/lib/checkout-submit-guards.test.ts src/lib/rpc-errors.test.ts src/lib/rpc.test.ts src/lib/order-stages.test.ts src/theme/layout.test.ts src/lib/golden-journey.test.ts src/lib/customer-projections.test.ts src/lib/product-publication.test.ts src/lib/buyer-commercial-validation.test.ts src/lib/buyer-communication-log.test.ts src/lib/general-query-idempotency.test.ts src/lib/quote-projections.test.ts src/lib/quote-guards.test.ts src/lib/quote-idempotency.test.ts src/lib/quote-rpc-binding-check.test.ts src/lib/p106-quote-authority.test.ts src/lib/genie-product-resolution.test.ts src/lib/genie-draft-line-commit.test.ts src/lib/payment-gateway-boundary.test.ts src/lib/payment-idempotency.test.ts src/lib/runtime-payment-gateway-binding.test.ts src/lib/buyer-deployment-rpc-allowlist.test.ts src/lib/session-routing.test.ts src/lib/buyer-identity-claim-core.test.ts src/lib/buyer-identity.test.ts src/lib/login-preflight-action.test.ts src/lib/deadlock-regression.test.ts src/lib/draft-utils.test.ts src/lib/buyer-session-refresh-triggers.test.ts src/lib/checkout-submit-error.test.ts scripts/buyer-mobile-certification/revenue-journey.integration.test.mjs src/services/customerGateway.test.ts",
+    "test": "node --import tsx --test src/lib/checkout-idempotency.test.ts src/lib/checkout-submit-guards.test.ts src/lib/rpc-errors.test.ts src/lib/rpc.test.ts src/lib/order-stages.test.ts src/theme/layout.test.ts src/lib/golden-journey.test.ts src/lib/customer-projections.test.ts src/lib/product-publication.test.ts src/lib/buyer-commercial-validation.test.ts src/lib/buyer-communication-log.test.ts src/lib/general-query-idempotency.test.ts src/lib/support-ticket-idempotency.test.ts src/lib/qu